# shows string slicing. 

name = 'Shashwat'
print(name[1:8:2])# this :: values are prameter. 

# first parameter shows the start Index
# second parameter shows the end index+1
# third parameter shows(to skip). its base value is 1. so it subtracts 1 from 3rd parameter